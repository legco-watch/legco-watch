#dev:
#  build: docker/dev
#  ports:
#    - "2223:22"
#  volumes:
#    - ./:/docker
#  volumes_from:
#    - dbdata
#  links:
#    - db
#
dbdata:
  build: docker/dbdata

db:
  build: docker/dbserver
  ports:
    - "5432:5432"
  volumes_from:
    - dbdata

appserver:
  build: docker/appserver
  links:
    - db
